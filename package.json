{
  "name": "split-tiff",
  "version": "0.0.0",
  "description": "Split a tiff file having multiple images into single image tiff files.",
  "private": true,
  "license": "WTFPL",
  "repository": "luncheon/split-tiff",
  "type": "module",
  "main": "index.js",
  "types": "index.d.ts",
  "bin": "cli.js",
  "scripts": {
    "build": "tsc -p . && biome lint",
    "watch": "tsc -p . -w --preserveWatchOutput"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "typescript": "^5.6.3"
  }
}
